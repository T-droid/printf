#include "main.h"

/**
 * print_flag - prints according to non custom conversion
 * @a: flag
 * @b: conversion aspecifier
 */
void print_flag(char a, char b)
{
	if (c == '+')
		putchar(c);
	else if (c == ' ')
		_putchar(c);
	else if (c == '#')
	{
		_putchar('0');
		if (specifier(b) == 'A')
		{
			_putchar('X');
		}
		else
			_putchar('x');
	}
}
